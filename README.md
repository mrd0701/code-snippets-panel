
# Code Snippets Panel

Code Snippets Panel — это расширение для VS Code, которое позволяет удобно хранить, редактировать и быстро копировать фрагменты кода (сниппеты) прямо в боковой панели.

## Основные возможности

- Боковая панель для хранения сниппетов
- Добавление нового сниппета:
- - Вручную через текстовое поле
- - Вставкой из буфера обмена
- Редактирование существующих сниппетов
- Удаление сниппетов
- Клик по сниппету → мгновенное копирование в буфер обмена
- Сниппеты сохраняются между сессиями
- Кнопка в нижней панели для быстрого открытия панели со снипетами

## Использование

1. В статус-баре внизу теперь есть кнопка для открытия боковой панели со снипетами:
   ```
   📘 Сниппеты
   ```

2. В панели доступны:
   - кнопка "Добавить" — открывает форму для ручного ввода нового снипета
   - кнопка "Вставить из буфера" — создаёт спипет из текущего содержимого буфера обмена
   - кнопка "Редактировать" — позволяет вручную редактировать содержимое существующего снипета
   - кнопка "Удалить" — удаляет снипет
   - \*Клик по сниппету\* — копирует содержимое снипета в буфер обмена 
   - Загрузить — обновить список сниппетов из локального хранилища
   - Поисковая панель "Фильтр" -  "Поиск снипетов по содержимому

## Совместимость

- VS Code ≥ 1.80
- Node.js ≥ 18

## Структура проекта
```
code-snippets-panel/
├─ package.json
├─ tsconfig.json
├─ src/
│  ├─ extension.ts
│  ├─ utils.ts
│  └─ panel/
│     └─ webview.html
└─ media/
   └─ icon.svg
```
